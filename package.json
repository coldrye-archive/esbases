{
  "name": "esbases",
  "version": "0.0.9",
  "description": "Assorted base classes extending native classes so that they can be subclassed properly when using Babel 6.x+",
  "keywords": [
    "native", "babel", "es2015", "error", "number", "subclass", "class", "extend", "es", "base"
  ],
  "license": "Apache-2.0",
  "homepage": "http://esbases.es.coldrye.eu/",
  "bugs": {
    "url": "https://github.com/coldrye-es/esbases/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/coldrye-es/esbases.git"
  },
  "author": {
    "name": "Carsten Klein",
    "email": "trancesilken@gmail.com"
  },
  "maintainers": [
    {
      "name": "cklein",
      "email": "trancesilken@gmail.com"
    }
  ],
  "files": [
    "*"
  ],
  "scripts": {
    "prepublish": "test \"${MASTER}\" = \"1\" || (echo ${npm_config_argv} | grep -E '(install|update)' >/dev/null 2>/dev/null || (echo 'use *make publish* instead' && exit 1))"
  },
  "devDependencies": {
    "babel-plugin-external-helpers-2": ">=6.3.13",
    "babel-plugin-transform-runtime": ">=6.8.0",
    "babel-plugin-undeclared-variables-check": ">=6.8.0",
    "babel-preset-es2015": ">=6.6.0",
    "babel-preset-stage-0": ">=6.5.0",
    "babel-runtime": ">=6.6.1",
    "chai": ">=3.5.0",
    "esdoc-es7-plugin": ">=0.0",
    "esdoc-importpath-plugin": ">=0.0",
    "esmake": "coldrye-es/esmake",
    "glob": ">=6.0.1",
    "sinon": ">=1.17.4",
    "sinon-chai": ">=2.8.0"
  },
  "globalDevDependencies": {
    "babel-cli": ">=6.8.0",
    "babel-core": ">=6.8.0",
    "babel-eslint": ">=6.0.4",
    "babel-istanbul": ">=0.8.0",
    "conventional-changelog-cli": ">=1.2.0",
    "esdoc": ">=0.4.7",
    "eslint": ">=2.9.0",
    "eslint-config-coldrye": ">=0.0.13",
    "mocha": ">=2.4.5"
  }
}
